var n1 = Number(window.prompt("Digite um número: ")) // prompt retorna script
var n2 = Number(window.prompt('Digite outro número: ')) // prompt retorna script
var s = n1 + n2
window.alert(`A soma entre ${n1} e ${n2} é igual a ${s}`)
